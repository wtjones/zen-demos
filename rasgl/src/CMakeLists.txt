add_subdirectory(core)
if (NOT RAS_PLATFORM_TYPE STREQUAL "embedded")
    add_subdirectory(hosted)
endif()
add_subdirectory(external)
IF (DEFINED RAS_PLATFORM)
    add_subdirectory(platform)
    add_subdirectory(demo)
endif()
