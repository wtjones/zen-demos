IF      !DEF(MAP_INC)
DEF MAP_INC = 1

DEF MAP_BITS_PER_TILE       EQU 8
DEF MAP_WIDTH               EQU 64
DEF MAP_HEIGHT              EQU 32
DEF MAP_BYTES_PER_ROW       EQU MAP_WIDTH * MAP_BITS_PER_TILE / 8

MACRO GENERATE_MAP_LOOKUP
DEF ROW =  0
    REPT MAP_HEIGHT
        db    HIGH(map_data + (ROW * MAP_BYTES_PER_ROW))
        db    LOW(map_data + (ROW * MAP_BYTES_PER_ROW))
DEF ROW = ROW + 1
    ENDR

    ENDM

ENDC    ;MAP_INC

